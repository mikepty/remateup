# Business Rules Engine — Configurable business rule processing
# engine.py     — Rule execution engine
# registry.py   — Rule registry
# conditions.py — Rule condition definitions
# actions.py    — Rule action definitions
